# Verglos Engine Source Policy

Engine acquisition is explicit. A verified pre-seeded cache wins; otherwise a
configured HTTPS mirror may be selected. HTTP, malformed, implicit, or
credential-bearing sources are unavailable, and offline mode never falls back
to the public internet. This policy selects a source only; manifest/signature
verification remains mandatory before activation.
