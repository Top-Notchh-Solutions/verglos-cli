# `verglos policy check`

`verglos policy check <evaluation>` reads a local policy-evaluation artifact,
renders its evidence-first explanation, and returns the recorded contract exit
code. `--json` and `--quiet` are supported; invalid input returns the typed
usage/integrity exit and the command performs no mutation.
