# Verglos Hunt Execution Gate

Hunt execution requires all independent gates: exact rule/subject match,
trusted non-revoked recipe, and an approved unexpired `execute` receipt bound to
the same subject. The gate authorizes only; it never starts a process.
