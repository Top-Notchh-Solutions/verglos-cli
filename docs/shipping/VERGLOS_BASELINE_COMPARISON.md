# Verglos Baseline Comparison

Baseline comparison separates current fingerprints already accepted by the
subject/policy-bound baseline from new fingerprints. A stale or mismatched
baseline remains an explicit status and is not treated as accepted debt.
