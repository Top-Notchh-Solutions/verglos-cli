# Verglos Policy Document

The versioned policy document defines checks and explicit behavior for
severity, confidence, freshness, coverage, artifact matching, Hunt evidence,
exceptions, approvals, and namespaced extensions. Unknown fields and unsafe
advisory-blocking combinations are rejected; the canonical document digest is
available for audit and downstream evaluation.
