# Verglos Release Diff

Snapshot diffs classify only stable fingerprint presence: added, fixed, and
unchanged. Identity, lineage coverage, and policy-input changes are separate
flags. The diff does not infer severity changes or claim that a finding was
resolved without matching evidence.
