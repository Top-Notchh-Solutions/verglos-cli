# Verglos MCP Capability Truth

The MCP capability registry records each legacy tool's plan boundary,
shipped/partial maturity, and approval requirement. It is derived from the
shared authority mapping so capability discovery cannot silently advertise a
side-effecting tool as read-only.
