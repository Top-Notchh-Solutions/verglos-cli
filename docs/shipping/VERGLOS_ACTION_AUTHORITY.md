# Verglos Action Authority

Agents are callers, not owners. Inspect and propose actions are approval-free
and side-effect-free. Mutate, install, execute, network, exception, signing,
publishing, upload, policy, and billing actions require explicit approval and
declare their side-effect class.
