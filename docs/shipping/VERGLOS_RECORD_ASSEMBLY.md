# Verglos Release Record Assembly

The assembly boundary requires exactly one required release-decision member,
then delegates ordering and strict validation to the Release Record manifest
contract. It does not sign, publish, or upload records.
