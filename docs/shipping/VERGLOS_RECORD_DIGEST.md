# Verglos Record Manifest Digest

Validated Release Record manifests receive a deterministic SHA-256 digest over
canonical JSON. This digest identifies the exact manifest bytes and does not
imply a signature or hosted publication.
