# `verglos engines status`

`verglos engines status` reads the user-controlled engine cache and reports
installed versions and computed digests. `--json` is intended for automation.
The command is read-only: it does not download, activate, update, or roll back
engine artifacts.
