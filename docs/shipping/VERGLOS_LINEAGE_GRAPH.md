# Verglos Source-to-Artifact Lineage Graph

The lineage graph connects immutable subject identities across source, build,
SBOM, image, and artifact evidence. Missing endpoints, mismatches, and
unverifiable declarations are retained as explicit gaps; the graph never
upgrades a declaration into provenance or silently drops an edge.
