# Verglos Release Provenance Predicate

The release predicate binds a validated manifest digest, exact subject IDs, and
recorded limitations in a stable predicate type. It does not claim a
cryptographic signature or external build provenance.
