# Verglos Baseline Contract

Baselines bind accepted observation fingerprints to an immutable subject and
policy digest. Duplicate fingerprints, invalid identities, and backwards
expiry windows are rejected. Comparison exposes matched, mismatched, or stale
state explicitly for policy evaluation.
