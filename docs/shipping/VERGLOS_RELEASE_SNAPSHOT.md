# Verglos Release Snapshot

A release snapshot freezes primary and related subject identities, correlated
observation fingerprints, lineage edges and gaps, and a digest of policy
inputs. The snapshot digest is derived from canonical bytes so comparison does
not depend on producer or presentation ordering.
